name: Pull request labeler
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Teste de execução
        run: echo Validacao de execucao!
